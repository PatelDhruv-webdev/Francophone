// Active theme — swap this import to redesign the entire app
// All components, CSS variables, and Tailwind tokens derive from here
export { cafeLumiere as activeTheme } from './themes/cafe-lumiere'
export type { Theme, ThemeColors, ThemeFonts } from './themes/theme.types'
